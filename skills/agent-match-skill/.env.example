# DATA_GOV_SG_API_KEY (optional, raises rate limit without it)
# Get a free key at https://data.gov.sg/ (register → My Account → API Key)
# The agent-match-skill works anonymously but with stricter rate limits.
DATA_GOV_SG_API_KEY=
